    //it's like arraylist (All under <stdlib.h> header file)
    /*
    1. malloc():  memory-allocation
                it returns a pointer of type void. Can be cast into pointer of any type
                int* ptr = (int*) malloc(size);

    2. calloc(): contiginous allocation (Even closer to Arraylist)
        ptr = (cast-type*)calloc(n, element-size);
        Defualt value is zero
    3. free()
    4. realloc()

    */